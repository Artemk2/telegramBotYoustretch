package com.youstretch.telegram.yclientsapi;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import org.youstretch.telegram.yclientsapi.YclientsApplication;

@RunWith(SpringRunner.class)
@SpringBootTest(classes = YclientsApplication.class)
class YclientsApplicationTests {

	@Test
	void contextLoads() {
		// Пустой тест, чтобы проверить, что контекст приложения загружается без ошибок
	}

}
